version: '3.9'

services:
   cli:
     build: .
     image: llslim/docker-phpcli:8
    #  secrets:
      #- user_ssh_key
      # - dev_ssh_key

# secrets:
  # user_ssh_key:
    # file: ~/.ssh/id_rsa
  # dev_ssh_key:
  #   file: ~/.ssh/kevdev_rsa